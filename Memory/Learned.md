# Learned

## 2026-06-03
- The Obsidian MCP endpoint requires Accept: application/json, text/event-stream; using only application/json returns HTTP 406.
- The Obsidian MCP exposes vault tools including vault_read, vault_write, and vault_append.
- Keeping the Obsidian-backed notes inside the repository's Memory/ folder makes the workflow visible to existing Selova Codex conversations through normal file reads.
